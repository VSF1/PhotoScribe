# Development / test dependencies (not needed to run the app).
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# Pillow is already a runtime dependency (see requirements.txt) and is used
# to generate image fixtures. The write tests also need ExifTool on PATH
# (or bundled); they skip themselves if it isn't found.
pytest>=8.0
